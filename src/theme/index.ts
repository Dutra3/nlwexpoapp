import { COLORS } from "./colors";
import { FONTS } from "./fonts";

export { COLORS, FONTS };

// Aqui existe essa folder para que eu de o export direto de theme
// Pois como sabemos, ele sempre vai buscar no index, não precisa especificar mais do que isso.
// Por conta dessa import e export aqui do index.